import java.util.Arrays;
class Solution {
  public int solution(int[] A) {
   	int num = 1;
	Arrays.sort(A);
	for (int i = 0 ; i < A.length; i++) {
		if(A[i]<=0) continue;
		if(A[i]==num) num++;
	}
  return num;
 }
}
